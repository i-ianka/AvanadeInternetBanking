import { PontoVirgulaPipe } from './ponto-virgula.pipe';

describe('PontoVirgulaPipe', () => {
  it('create an instance', () => {
    const pipe = new PontoVirgulaPipe();
    expect(pipe).toBeTruthy();
  });
});
